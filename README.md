# JavaScript-Projects

variable: It is used to store the data in Javascript.

- we use var, let, const to store the data.

Hoisting: Moving declarations or functions to the top.
function abc(){
console.log("function");
}
abc(); //function
